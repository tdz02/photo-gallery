<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title><%= title || "Login" %></title>

    <link
        rel="stylesheet"
        href="/css/style.css"
    >
</head>

<body>

    <main class="auth-page">

        <section class="auth-card">

            <header class="auth-header">

                <h1>Welcome back</h1>

                <p>
                    Log in to upload and manage your photos.
                </p>

            </header>

            <% if (errors && errors.length > 0) { %>

                <div
                    class="auth-errors"
                    role="alert"
                >

                    <ul>

                        <% errors.forEach((error) => { %>

                            <li>
                                <%= error %>
                            </li>

                        <% }) %>

                    </ul>

                </div>

            <% } %>

            <form
                action="/auth/login"
                method="POST"
                class="auth-form"
            >

                <div class="form-group">

                    <label for="email">
                        Email
                    </label>

                    <input
                        type="email"
                        id="email"
                        name="email"
                        value="<%= oldInput?.email || "" %>"
                        autocomplete="email"
                        required
                    >

                </div>

                <div class="form-group">

                    <label for="password">
                        Password
                    </label>

                    <input
                        type="password"
                        id="password"
                        name="password"
                        autocomplete="current-password"
                        required
                    >

                </div>

                <button
                    type="submit"
                    class="auth-submit-button"
                >
                    Log in
                </button>

            </form>

            <p class="auth-switch">

                Do not have an account?

                <a href="/auth/register">
                    Create account
                </a>

            </p>

        </section>

    </main>

</body>

</html>